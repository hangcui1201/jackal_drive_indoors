shutdown_costmaps: false

controller_frequency: 20.0
controller_patience: 15.0

planner_frequency: 20.0
planner_patience: 5.0

oscillation_timeout: 0.0
oscillation_distance: 0.5

recovery_behavior_enabled: true
clearing_rotation_allowed: true # It will turn around to clear space if for some reason it gets lost to try and recover itself
                                # Its also usefull for situations when a moving obstacle was detected in its path. By turning 
                                # it gives time to the obstacle to move out the way and therefore clear the localcostmap.
